{
  "_description": "Replace with ComfyUI API export for Wan 2.2 text-to-video.",
  "inputs": {
    "prompt": "{{prompt}}",
    "negative_prompt": "{{negative_prompt}}",
    "seed": "{{seed}}"
  }
}

